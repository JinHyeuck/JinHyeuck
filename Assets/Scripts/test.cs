using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class test : MonoBehaviour
{
	//private Slider slider;
	//private Character character;

	void Start () {
		//slider = gameObject.GetComponent<Slider> ();
		print("sdfs");
	}
	
	// Update is called once per frame
	void Update ()
	{
		//slider.transform.position = character.transform.position;
		//slider.transform.Translate (0.0f, 5.0f, 0.0f);
	}

	public void bindCharacter(Character _char) {
		//character = _char;
	}
}

